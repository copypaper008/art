'use strict';

// iframe poster overlay replaced by canvas-drawn portrait morphing in sketch.js
let alarmOverlayActive = false;

function updateAlarmPosters(_subjects) {
  // Intentionally empty — alarm sequence now drawn directly on the p5 canvas
  // by drawAlarmTransformation() in sketch.js
}
