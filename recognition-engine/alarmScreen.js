'use strict';

// ALARM rendering is handled directly on the p5.js canvas by
// drawAlarmTransformation() in sketch.js — no iframe overlay needed.
let alarmOverlayActive = false;

function updateAlarmPosters(_subjects) {}
